// SNIPPETS_START template_kickstart_interactive
#include <bits/stdc++.h>

using namespace std;


void solve() {
    ${0:${VISUAL}}
}

// keep in mind what format needed!! might WA!
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int T; cin >> T;
    for (int t = 1; t <= T; t++) {
        solve();
        int verdict; cin >> verdict;
        assert(verdict == 1);
    }
    return 0;
}
// SNIPPETS_END
