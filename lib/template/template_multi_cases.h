// SNIPPETS_START template_multi_cases
#include <bits/stdc++.h>

using namespace std;


void solve() {
    ${0:${VISUAL}}
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int T; cin >> T;
    for (int t = 0; t < T; t++) {
        solve();
    }
    return 0;
}
// SNIPPETS_END
